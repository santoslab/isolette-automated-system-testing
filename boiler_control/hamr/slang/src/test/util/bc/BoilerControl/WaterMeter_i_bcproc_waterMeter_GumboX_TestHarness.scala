// #Sireum

package bc.BoilerControl

import org.sireum._
import bc._
import bc.GumboXUtil.GumboXResult

// Do not edit this file as it will be overwritten if HAMR codegen is rerun
@msig trait WaterMeter_i_bcproc_waterMeter_GumboX_TestHarness extends WaterMeter_i_bcproc_waterMeter_TestApi {
  def verbose: B

  /** Contract-based test harness for the initialise entry point
    */
  def testInitialiseCB(
      ): GumboXResult.Type = {

    if (verbose) {
      println(st"""Pre State Values:
                  """.render)
    }

    // [InvokeEntryPoint]: invoke the entry point test method
    testInitialise()

    // [RetrieveOutState]: retrieve values of the output ports via get operations and GUMBO declared local state variable
    val api_ready: Option[art.Empty] = get_ready()
    val api_level: Option[Base_Types.Float_32] = get_level()
    val volume: Base_Types.Float_32 = bc.BoilerControl.WaterMeter_i_bcproc_waterMeter.volume

    if (verbose) {
      println(st"""Post State Values:
                  |  api_ready = ${api_ready.string}
                  |  api_level = ${api_level.string}
                  |  volume = ${volume.string}""".render)
    }

    // [CheckPost]: invoke the oracle function
    val postResult = bc.BoilerControl.WaterMeter_i_bcproc_waterMeter_GumboX.inititialize_IEP_Post(volume, api_ready, api_level)
    val result: GumboXResult.Type =
      if (!postResult) GumboXResult.Post_Condition_Fail
      else GumboXResult.Post_Condition_Pass

    return result
  }

  def testComputeCBJ(json: String): GumboXResult.Type = {
    bc.JSON.toBoilerControlWaterMeter_i_bcproc_waterMeter_PreState_Container(json) match {
      case Either.Left(o) => return testComputeCBV(o)
      case Either.Right(msg) => halt(msg.string)
    }
  }

  def testComputeCBV(o: WaterMeter_i_bcproc_waterMeter_PreState_Container): GumboXResult.Type = {
    return testComputeCB(o.api_programReady, o.api_opMode)
  }

  /** Contract-based test harness for the compute entry point
    * @param api_programReady incoming event port
    * @param api_opMode incoming event data port
    */
  def testComputeCB(
      api_programReady: Option[art.Empty],
      api_opMode: Option[BoilerControl.OpMode.Type]): GumboXResult.Type = {

    // [SaveInLocal]: retrieve and save the current (input) values of GUMBO-declared local state variables as retrieved from the component state
    val In_volume: Base_Types.Float_32 = bc.BoilerControl.WaterMeter_i_bcproc_waterMeter.volume

    // [CheckPre]: check/filter based on pre-condition.
    //   waterMeter's compute entry point does not have top level assume clauses

    // [PutInPorts]: put values on the input ports
    if (api_programReady.nonEmpty) {
      put_programReady()
    }
    if (api_opMode.nonEmpty) {
      put_opMode(api_opMode.get)
    }

    if (verbose) {
      println(st"""Pre State Values:
                  |  In_volume = ${In_volume.string}
                  |  api_programReady = ${api_programReady.string}
                  |  api_opMode = ${api_opMode.string}""".render)
    }

    // [InvokeEntryPoint]: invoke the entry point test method
    testCompute()

    // [RetrieveOutState]: retrieve values of the output ports via get operations and GUMBO declared local state variable
    val api_ready: Option[art.Empty] = get_ready()
    val api_level: Option[Base_Types.Float_32] = get_level()
    val volume: Base_Types.Float_32 = bc.BoilerControl.WaterMeter_i_bcproc_waterMeter.volume

    if (verbose) {
      println(st"""Post State Values:
                  |  api_ready = ${api_ready.string}
                  |  api_level = ${api_level.string}
                  |  volume = ${volume.string}""".render)
    }

    // [CheckPost]: invoke the oracle function
    val postResult = bc.BoilerControl.WaterMeter_i_bcproc_waterMeter_GumboX.compute_CEP_Post(In_volume, volume, api_programReady, api_ready, api_level)
    val result: GumboXResult.Type =
      if (!postResult) GumboXResult.Post_Condition_Fail
      else GumboXResult.Post_Condition_Pass

    return result
  }

  def testComputeCBwLJ(json: String): GumboXResult.Type = {
    bc.JSON.toBoilerControlWaterMeter_i_bcproc_waterMeter_PreState_Container_PS(json) match {
      case Either.Left(o) => return testComputeCBwLV(o)
      case Either.Right(msg) => halt(msg.string)
    }
  }

  def testComputeCBwLV(o: WaterMeter_i_bcproc_waterMeter_PreState_Container_PS): GumboXResult.Type = {
    return testComputeCBwL(o.In_volume, o.api_programReady, o.api_opMode)
  }

  /** Contract-based test harness for the compute entry point
    * @param In_volume pre-state state variable
    * @param api_programReady incoming event port
    * @param api_opMode incoming event data port
    */
  def testComputeCBwL(
      In_volume: Base_Types.Float_32,
      api_programReady: Option[art.Empty],
      api_opMode: Option[BoilerControl.OpMode.Type]): GumboXResult.Type = {

    // [CheckPre]: check/filter based on pre-condition.
    //   waterMeter's compute entry point does not have top level assume clauses

    // [PutInPorts]: put values on the input ports
    if (api_programReady.nonEmpty) {
      put_programReady()
    }
    if (api_opMode.nonEmpty) {
      put_opMode(api_opMode.get)
    }

    // [SetInStateVars]: set the pre-state values of state variables
    bc.BoilerControl.WaterMeter_i_bcproc_waterMeter.volume = In_volume

    if (verbose) {
      println(st"""Pre State Values:
                  |  In_volume = ${In_volume.string}
                  |  api_programReady = ${api_programReady.string}
                  |  api_opMode = ${api_opMode.string}""".render)
    }

    // [InvokeEntryPoint]: invoke the entry point test method
    testCompute()

    // [RetrieveOutState]: retrieve values of the output ports via get operations and GUMBO declared local state variable
    val api_ready: Option[art.Empty] = get_ready()
    val api_level: Option[Base_Types.Float_32] = get_level()
    val volume: Base_Types.Float_32 = bc.BoilerControl.WaterMeter_i_bcproc_waterMeter.volume

    if (verbose) {
      println(st"""Post State Values:
                  |  api_ready = ${api_ready.string}
                  |  api_level = ${api_level.string}
                  |  volume = ${volume.string}""".render)
    }

    // [CheckPost]: invoke the oracle function
    val postResult = bc.BoilerControl.WaterMeter_i_bcproc_waterMeter_GumboX.compute_CEP_Post(In_volume, volume, api_programReady, api_ready, api_level)
    val result: GumboXResult.Type =
      if (!postResult) GumboXResult.Post_Condition_Fail
      else GumboXResult.Post_Condition_Pass

    return result
  }
}
