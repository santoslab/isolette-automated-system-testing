// #Sireum

package bc.BoilerControl

import org.sireum._
import bc._
import bc.GumboXUtil.GumboXResult

// Do not edit this file as it will be overwritten if HAMR codegen is rerun
@msig trait SteamMeter_i_bcproc_steamMeter_GumboX_TestHarness extends SteamMeter_i_bcproc_steamMeter_TestApi {
  def verbose: B

  /** Contract-based test harness for the initialise entry point
    */
  def testInitialiseCB(
      ): GumboXResult.Type = {

    if (verbose) {
      println(st"""Pre State Values:
                  """.render)
    }

    // [InvokeEntryPoint]: invoke the entry point test method
    testInitialise()

    // [RetrieveOutState]: retrieve values of the output ports via get operations and GUMBO declared local state variable
    val api_ready: Option[art.Empty] = get_ready()
    val api_rate: Option[Base_Types.Float_32] = get_rate()
    val computedRate: Base_Types.Float_32 = bc.BoilerControl.SteamMeter_i_bcproc_steamMeter.computedRate
    val volume: Base_Types.Float_32 = bc.BoilerControl.SteamMeter_i_bcproc_steamMeter.volume

    if (verbose) {
      println(st"""Post State Values:
                  |  api_ready = ${api_ready.string}
                  |  api_rate = ${api_rate.string}
                  |  computedRate = ${computedRate.string}
                  |  volume = ${volume.string}""".render)
    }

    // [CheckPost]: invoke the oracle function
    val postResult = bc.BoilerControl.SteamMeter_i_bcproc_steamMeter_GumboX.inititialize_IEP_Post(computedRate, volume, api_ready, api_rate)
    val result: GumboXResult.Type =
      if (!postResult) GumboXResult.Post_Condition_Fail
      else GumboXResult.Post_Condition_Pass

    return result
  }

  def testComputeCBJ(json: String): GumboXResult.Type = {
    bc.JSON.toBoilerControlSteamMeter_i_bcproc_steamMeter_PreState_Container(json) match {
      case Either.Left(o) => return testComputeCBV(o)
      case Either.Right(msg) => halt(msg.string)
    }
  }

  def testComputeCBV(o: SteamMeter_i_bcproc_steamMeter_PreState_Container): GumboXResult.Type = {
    return testComputeCB(o.api_programReady, o.api_opMode)
  }

  /** Contract-based test harness for the compute entry point
    * @param api_programReady incoming event port
    * @param api_opMode incoming event data port
    */
  def testComputeCB(
      api_programReady: Option[art.Empty],
      api_opMode: Option[BoilerControl.OpMode.Type]): GumboXResult.Type = {

    // [SaveInLocal]: retrieve and save the current (input) values of GUMBO-declared local state variables as retrieved from the component state
    val In_computedRate: Base_Types.Float_32 = bc.BoilerControl.SteamMeter_i_bcproc_steamMeter.computedRate
    val In_volume: Base_Types.Float_32 = bc.BoilerControl.SteamMeter_i_bcproc_steamMeter.volume

    // [CheckPre]: check/filter based on pre-condition.
    //   steamMeter's compute entry point does not have top level assume clauses

    // [PutInPorts]: put values on the input ports
    if (api_programReady.nonEmpty) {
      put_programReady()
    }
    if (api_opMode.nonEmpty) {
      put_opMode(api_opMode.get)
    }

    if (verbose) {
      println(st"""Pre State Values:
                  |  In_computedRate = ${In_computedRate.string}
                  |  In_volume = ${In_volume.string}
                  |  api_programReady = ${api_programReady.string}
                  |  api_opMode = ${api_opMode.string}""".render)
    }

    // [InvokeEntryPoint]: invoke the entry point test method
    testCompute()

    // [RetrieveOutState]: retrieve values of the output ports via get operations and GUMBO declared local state variable
    val api_ready: Option[art.Empty] = get_ready()
    val api_rate: Option[Base_Types.Float_32] = get_rate()
    val computedRate: Base_Types.Float_32 = bc.BoilerControl.SteamMeter_i_bcproc_steamMeter.computedRate
    val volume: Base_Types.Float_32 = bc.BoilerControl.SteamMeter_i_bcproc_steamMeter.volume

    if (verbose) {
      println(st"""Post State Values:
                  |  api_ready = ${api_ready.string}
                  |  api_rate = ${api_rate.string}
                  |  computedRate = ${computedRate.string}
                  |  volume = ${volume.string}""".render)
    }

    // [CheckPost]: invoke the oracle function
    val postResult = bc.BoilerControl.SteamMeter_i_bcproc_steamMeter_GumboX.compute_CEP_Post(In_computedRate, In_volume, computedRate, volume, api_programReady, api_ready, api_rate)
    val result: GumboXResult.Type =
      if (!postResult) GumboXResult.Post_Condition_Fail
      else GumboXResult.Post_Condition_Pass

    return result
  }

  def testComputeCBwLJ(json: String): GumboXResult.Type = {
    bc.JSON.toBoilerControlSteamMeter_i_bcproc_steamMeter_PreState_Container_PS(json) match {
      case Either.Left(o) => return testComputeCBwLV(o)
      case Either.Right(msg) => halt(msg.string)
    }
  }

  def testComputeCBwLV(o: SteamMeter_i_bcproc_steamMeter_PreState_Container_PS): GumboXResult.Type = {
    return testComputeCBwL(o.In_computedRate, o.In_volume, o.api_programReady, o.api_opMode)
  }

  /** Contract-based test harness for the compute entry point
    * @param In_computedRate pre-state state variable
    * @param In_volume pre-state state variable
    * @param api_programReady incoming event port
    * @param api_opMode incoming event data port
    */
  def testComputeCBwL(
      In_computedRate: Base_Types.Float_32,
      In_volume: Base_Types.Float_32,
      api_programReady: Option[art.Empty],
      api_opMode: Option[BoilerControl.OpMode.Type]): GumboXResult.Type = {

    // [CheckPre]: check/filter based on pre-condition.
    //   steamMeter's compute entry point does not have top level assume clauses

    // [PutInPorts]: put values on the input ports
    if (api_programReady.nonEmpty) {
      put_programReady()
    }
    if (api_opMode.nonEmpty) {
      put_opMode(api_opMode.get)
    }

    // [SetInStateVars]: set the pre-state values of state variables
    bc.BoilerControl.SteamMeter_i_bcproc_steamMeter.computedRate = In_computedRate
    bc.BoilerControl.SteamMeter_i_bcproc_steamMeter.volume = In_volume

    if (verbose) {
      println(st"""Pre State Values:
                  |  In_computedRate = ${In_computedRate.string}
                  |  In_volume = ${In_volume.string}
                  |  api_programReady = ${api_programReady.string}
                  |  api_opMode = ${api_opMode.string}""".render)
    }

    // [InvokeEntryPoint]: invoke the entry point test method
    testCompute()

    // [RetrieveOutState]: retrieve values of the output ports via get operations and GUMBO declared local state variable
    val api_ready: Option[art.Empty] = get_ready()
    val api_rate: Option[Base_Types.Float_32] = get_rate()
    val computedRate: Base_Types.Float_32 = bc.BoilerControl.SteamMeter_i_bcproc_steamMeter.computedRate
    val volume: Base_Types.Float_32 = bc.BoilerControl.SteamMeter_i_bcproc_steamMeter.volume

    if (verbose) {
      println(st"""Post State Values:
                  |  api_ready = ${api_ready.string}
                  |  api_rate = ${api_rate.string}
                  |  computedRate = ${computedRate.string}
                  |  volume = ${volume.string}""".render)
    }

    // [CheckPost]: invoke the oracle function
    val postResult = bc.BoilerControl.SteamMeter_i_bcproc_steamMeter_GumboX.compute_CEP_Post(In_computedRate, In_volume, computedRate, volume, api_programReady, api_ready, api_rate)
    val result: GumboXResult.Type =
      if (!postResult) GumboXResult.Post_Condition_Fail
      else GumboXResult.Post_Condition_Pass

    return result
  }
}
