// #Sireum

package bc.BoilerControl

import org.sireum._
import art._
import bc._
import bc.BoilerControl.{PumpControl_i_bcproc_pumpControl2 => component}

// Do not edit this file as it will be overwritten if HAMR codegen is rerun

@datatype class PumpControl_i_bcproc_pumpControl2_Bridge(
  val id: Art.BridgeId,
  val name: String,
  val dispatchProtocol: DispatchPropertyProtocol,
  val dispatchTriggers: Option[ISZ[Art.PortId]],

  opMode: Port[BoilerControl.OpMode.Type],
  openPump: Port[Base_Types.Boolean],
  pumpOpen: Port[Base_Types.Boolean],
  pumpFlow: Port[Base_Types.Boolean],
  programReady: Port[art.Empty],
  ready: Port[art.Empty]
  ) extends Bridge {

  val ports : Bridge.Ports = Bridge.Ports(
    dataIns = ISZ[art.UPort](),

    dataOuts = ISZ[art.UPort](),

    eventIns = ISZ[art.UPort](opMode,
                              openPump,
                              programReady),

    eventOuts = ISZ[art.UPort](pumpOpen,
                               pumpFlow,
                               ready)
  )

  val initialization_api : PumpControl_i_Initialization_Api = {
    val api = PumpControl_i_Initialization_Api(
      id,
      opMode.id,
      openPump.id,
      pumpOpen.id,
      pumpFlow.id,
      programReady.id,
      ready.id
    )
    PumpControl_i_bcproc_pumpControl2_Bridge.c_initialization_api = Some(api)
    api
  }

  val operational_api : PumpControl_i_Operational_Api = {
    val api = PumpControl_i_Operational_Api(
      id,
      opMode.id,
      openPump.id,
      pumpOpen.id,
      pumpFlow.id,
      programReady.id,
      ready.id
    )
    PumpControl_i_bcproc_pumpControl2_Bridge.c_operational_api = Some(api)
    api
  }

  val entryPoints : Bridge.EntryPoints =
    PumpControl_i_bcproc_pumpControl2_Bridge.EntryPoints(
      id,

      opMode.id,
      openPump.id,
      pumpOpen.id,
      pumpFlow.id,
      programReady.id,
      ready.id,

      dispatchTriggers,

      initialization_api,
      operational_api)
}

object PumpControl_i_bcproc_pumpControl2_Bridge {

  var c_initialization_api: Option[PumpControl_i_Initialization_Api] = None()
  var c_operational_api: Option[PumpControl_i_Operational_Api] = None()

  @datatype class EntryPoints(
    PumpControl_i_bcproc_pumpControl2_BridgeId : Art.BridgeId,
    opMode_Id : Art.PortId,
    openPump_Id : Art.PortId,
    pumpOpen_Id : Art.PortId,
    pumpFlow_Id : Art.PortId,
    programReady_Id : Art.PortId,
    ready_Id : Art.PortId,
    dispatchTriggers : Option[ISZ[Art.PortId]],
    initialization_api: PumpControl_i_Initialization_Api,
    operational_api: PumpControl_i_Operational_Api) extends Bridge.EntryPoints {

    val dataInPortIds: ISZ[Art.PortId] = IS()

    val eventInPortIds: ISZ[Art.PortId] = IS(opMode_Id,
                                             openPump_Id,
                                             programReady_Id)

    val dataOutPortIds: ISZ[Art.PortId] = IS()

    val eventOutPortIds: ISZ[Art.PortId] = IS(pumpOpen_Id,
                                              pumpFlow_Id,
                                              ready_Id)

    def initialise(): Unit = {
      PumpControl_i_bcproc_pumpControl2_EntryPoint_Companion.pre_initialise()

      // implement the following method in 'component':  def initialise(api: PumpControl_i_Initialization_Api): Unit = {}
      component.initialise(initialization_api)

      PumpControl_i_bcproc_pumpControl2_EntryPoint_Companion.post_initialise()

      Art.sendOutput(eventOutPortIds, dataOutPortIds)
    }

    def compute(): Unit = {
      PumpControl_i_bcproc_pumpControl2_Injection_Service.pre_receiveInput()

      Art.receiveInput(eventInPortIds, dataInPortIds)

      PumpControl_i_bcproc_pumpControl2_EntryPoint_Companion.pre_compute()

      // implement the following in 'component':  def timeTriggered(api: PumpControl_i_Operational_Api): Unit = {}
      component.timeTriggered(operational_api)

      PumpControl_i_bcproc_pumpControl2_EntryPoint_Companion.post_compute()

      Art.sendOutput(eventOutPortIds, dataOutPortIds)
    }

    def activate(): Unit = {
      // implement the following method in 'component':  def activate(api: PumpControl_i_Operational_Api): Unit = {}
      component.activate(operational_api)
    }

    def deactivate(): Unit = {
      // implement the following method in 'component':  def deactivate(api: PumpControl_i_Operational_Api): Unit = {}
      component.deactivate(operational_api)
    }

    def finalise(): Unit = {
      // implement the following method in 'component':  def finalise(api: PumpControl_i_Operational_Api): Unit = {}
      component.finalise(operational_api)
    }

    def recover(): Unit = {
      // implement the following method in 'component':  def recover(api: PumpControl_i_Operational_Api): Unit = {}
      component.recover(operational_api)
    }

    override
    def testInitialise(): Unit = {
      // implement the following method in 'component':  def initialise(api: PumpControl_i_Initialization_Api): Unit = {}
      component.initialise(initialization_api)
      Art.releaseOutput(eventOutPortIds, dataOutPortIds)
    }

    override
    def testCompute(): Unit = {
      Art.receiveInput(eventInPortIds, dataInPortIds)

      // implement the following in 'component':  def timeTriggered(api: PumpControl_i_Operational_Api): Unit = {}
      component.timeTriggered(operational_api)

      Art.releaseOutput(eventOutPortIds, dataOutPortIds)
    }
  }
}