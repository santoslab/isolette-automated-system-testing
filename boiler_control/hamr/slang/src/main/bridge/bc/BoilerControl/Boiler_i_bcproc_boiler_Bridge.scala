// #Sireum

package bc.BoilerControl

import org.sireum._
import art._
import bc._
import bc.BoilerControl.{Boiler_i_bcproc_boiler => component}

// Do not edit this file as it will be overwritten if HAMR codegen is rerun

@datatype class Boiler_i_bcproc_boiler_Bridge(
  val id: Art.BridgeId,
  val name: String,
  val dispatchProtocol: DispatchPropertyProtocol,
  val dispatchTriggers: Option[ISZ[Art.PortId]],

  opMode: Port[BoilerControl.OpMode.Type],
  programReady: Port[art.Empty],
  valve: Port[art.Empty],
  waiting: Port[art.Empty],
  ready: Port[art.Empty]
  ) extends Bridge {

  val ports : Bridge.Ports = Bridge.Ports(
    dataIns = ISZ[art.UPort](),

    dataOuts = ISZ[art.UPort](),

    eventIns = ISZ[art.UPort](opMode,
                              programReady,
                              valve),

    eventOuts = ISZ[art.UPort](waiting,
                               ready)
  )

  val initialization_api : Boiler_i_Initialization_Api = {
    val api = Boiler_i_Initialization_Api(
      id,
      opMode.id,
      programReady.id,
      valve.id,
      waiting.id,
      ready.id
    )
    Boiler_i_bcproc_boiler_Bridge.c_initialization_api = Some(api)
    api
  }

  val operational_api : Boiler_i_Operational_Api = {
    val api = Boiler_i_Operational_Api(
      id,
      opMode.id,
      programReady.id,
      valve.id,
      waiting.id,
      ready.id
    )
    Boiler_i_bcproc_boiler_Bridge.c_operational_api = Some(api)
    api
  }

  val entryPoints : Bridge.EntryPoints =
    Boiler_i_bcproc_boiler_Bridge.EntryPoints(
      id,

      opMode.id,
      programReady.id,
      valve.id,
      waiting.id,
      ready.id,

      dispatchTriggers,

      initialization_api,
      operational_api)
}

object Boiler_i_bcproc_boiler_Bridge {

  var c_initialization_api: Option[Boiler_i_Initialization_Api] = None()
  var c_operational_api: Option[Boiler_i_Operational_Api] = None()

  @datatype class EntryPoints(
    Boiler_i_bcproc_boiler_BridgeId : Art.BridgeId,
    opMode_Id : Art.PortId,
    programReady_Id : Art.PortId,
    valve_Id : Art.PortId,
    waiting_Id : Art.PortId,
    ready_Id : Art.PortId,
    dispatchTriggers : Option[ISZ[Art.PortId]],
    initialization_api: Boiler_i_Initialization_Api,
    operational_api: Boiler_i_Operational_Api) extends Bridge.EntryPoints {

    val dataInPortIds: ISZ[Art.PortId] = IS()

    val eventInPortIds: ISZ[Art.PortId] = IS(opMode_Id,
                                             programReady_Id,
                                             valve_Id)

    val dataOutPortIds: ISZ[Art.PortId] = IS()

    val eventOutPortIds: ISZ[Art.PortId] = IS(waiting_Id,
                                              ready_Id)

    def initialise(): Unit = {
      Boiler_i_bcproc_boiler_EntryPoint_Companion.pre_initialise()

      // implement the following method in 'component':  def initialise(api: Boiler_i_Initialization_Api): Unit = {}
      component.initialise(initialization_api)

      Boiler_i_bcproc_boiler_EntryPoint_Companion.post_initialise()

      Art.sendOutput(eventOutPortIds, dataOutPortIds)
    }

    def compute(): Unit = {
      Boiler_i_bcproc_boiler_Injection_Service.pre_receiveInput()

      Art.receiveInput(eventInPortIds, dataInPortIds)

      Boiler_i_bcproc_boiler_EntryPoint_Companion.pre_compute()

      // implement the following in 'component':  def timeTriggered(api: Boiler_i_Operational_Api): Unit = {}
      component.timeTriggered(operational_api)

      Boiler_i_bcproc_boiler_EntryPoint_Companion.post_compute()

      Art.sendOutput(eventOutPortIds, dataOutPortIds)
    }

    def activate(): Unit = {
      // implement the following method in 'component':  def activate(api: Boiler_i_Operational_Api): Unit = {}
      component.activate(operational_api)
    }

    def deactivate(): Unit = {
      // implement the following method in 'component':  def deactivate(api: Boiler_i_Operational_Api): Unit = {}
      component.deactivate(operational_api)
    }

    def finalise(): Unit = {
      // implement the following method in 'component':  def finalise(api: Boiler_i_Operational_Api): Unit = {}
      component.finalise(operational_api)
    }

    def recover(): Unit = {
      // implement the following method in 'component':  def recover(api: Boiler_i_Operational_Api): Unit = {}
      component.recover(operational_api)
    }

    override
    def testInitialise(): Unit = {
      // implement the following method in 'component':  def initialise(api: Boiler_i_Initialization_Api): Unit = {}
      component.initialise(initialization_api)
      Art.releaseOutput(eventOutPortIds, dataOutPortIds)
    }

    override
    def testCompute(): Unit = {
      Art.receiveInput(eventInPortIds, dataInPortIds)

      // implement the following in 'component':  def timeTriggered(api: Boiler_i_Operational_Api): Unit = {}
      component.timeTriggered(operational_api)

      Art.releaseOutput(eventOutPortIds, dataOutPortIds)
    }
  }
}