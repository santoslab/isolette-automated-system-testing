package tc.system_tests

import org.sireum._
import tc._

// Do not edit this file as it will be overwritten if SystemTestArtifactGen is rerun

object TempControl_Inputs_Container_SlangCheck {

  case class NameProvider(name: String,
                          function: (Any, Any) => B)

  case class TestRow(testMethod: NameProvider,
                     testDescription: String,
                     profile: TempControl_Inputs_Container_Profile,
                     preStateCheck: (Any => B),
                     property: NameProvider)
}

trait TempControl_Inputs_Container_SlangCheck
  extends SystemTestSuite {


  def next(profile: TempControl_Inputs_Container_Profile): Option[TempControl_Inputs_Container] = {
    return TempControl_Inputs_Container_Profile.nextH(profile)
  }

  def freshRandomLib: RandomLib = {
    return TempControl_Inputs_Container_Util.freshRandomLib
  }

  def getProfiles: MSZ[TempControl_Inputs_Container_Profile] = {
    return MSZ(getDefaultProfile)
  }

  //------------------------------------------------
  //  Test Vector Profiles
  //
  //   ..eventually auto-generated from a descriptor
  //     of injection vector
  //------------------------------------------------

  def getDefaultProfile: TempControl_Inputs_Container_Profile = {
    return TempControl_Inputs_Container_Profile.getDefaultProfile
  }

  def disableLogsAndGuis(): Unit = {

    // disable the various guis
    System.setProperty("java.awt.headless", "true")

    // suppress ART's log stream
    art.ArtNative_Ext.logStream = new java.io.PrintStream(new java.io.OutputStream {
      override def write(b: Int): Unit = {}
    })

    // suppress the static scheduler's log stream
    art.scheduling.static.StaticSchedulerIO_Ext.logStream = new java.io.PrintStream(new java.io.OutputStream {
      override def write(b: Int): Unit = {}
    })
  }
}
